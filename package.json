{
  "name": "@ipld/fbl",
  "version": "0.0.0-dev",
  "description": "`Flexible Byte Layout` is an advanced layout for representing binary data.",
  "main": "src/index.js",
  "scripts": {
    "build:schema": "ipld-schema to-json README.md > src/schema.json",
    "coverage": "nyc --reporter=html mocha test/test-*.js && npx http-server coverage",
    "pretest": "standard",
    "test": "hundreds mocha test/test-*.js && npm run test:browser",
    "test:browser": "polendina --cleanup test/test-*.js",
    "test:node": "mocha test/test-*.js"
  },
  "keywords": [],
  "author": "Mikeal Rogers <mikeal.rogers@gmail.com> (https://www.mikealrogers.com/)",
  "license": "(Apache-2.0 AND MIT)",
  "dependencies": {
    "@ipld/block": "^4.0.0",
    "@ipld/schema-validation": "0.0.0",
    "buffer": "^5.6.0"
  },
  "devDependencies": {
    "hundreds": "0.0.2",
    "ipld-schema": "^0.3.2",
    "mocha": "^7.1.1",
    "polendina": "^1.0.0",
    "standard": "^14.3.3"
  }
}
